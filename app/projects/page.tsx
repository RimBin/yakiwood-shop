export const metadata = { title: 'Projects - Yakiwood' };

export default function ProjectsPage() {
  return (
    <main className="min-h-screen p-10">
      <h1 className="text-3xl font-bold">Projects</h1>
      <p className="mt-4">Placeholder for projects and case studies. Add images to `public/` and reference them using `import` or a static path.</p>
    </main>
  );
}
