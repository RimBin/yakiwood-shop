# Keep Copilot context focused; avoid generated folders and especially problematic large binaries.
# Note: Copilot can't meaningfully interpret `.glb` contents (binary), but it can still see that files exist/changed.

# Build/test outputs
node_modules/
.next/
out/
coverage/
test-results/
playwright-report/

# 3D assets: keep `.glb` included, but ignore backup/extra-heavy formats
public/models/**/*.glb.bak
public/models/**/*.hdr
public/models/**/*.exr
